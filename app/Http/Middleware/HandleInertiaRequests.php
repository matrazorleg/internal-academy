<?php

namespace App\Http\Middleware;

use Illuminate\Http\Request;
use Inertia\Middleware;

class HandleInertiaRequests extends Middleware
{
    /**
     * The root template that is loaded on the first page visit.
     *
     * @var string
     */
    protected $rootView = 'app';

    /**
     * Determine the current asset version.
     */
    public function version(Request $request): ?string
    {
        return parent::version($request);
    }

    /**
     * Define the props that are shared by default.
     *
     * @return array<string, mixed>
     */
    public function share(Request $request): array
    {
        return array_merge(parent::share($request), [
            'auth' => [
                'user' => $request->user()
                    ? [
                        'id' => $request->user()->id,
                        'name' => $request->user()->name,
                        'email' => $request->user()->email,
                        'role' => $request->user()->role?->value ?? $request->user()->role,
                    ]
                    : null,
            ],
            'urls' => [
                'login' => route('login'),
                'loginStore' => route('login.store'),
                'logout' => route('logout'),
                'dashboard' => route('dashboard'),
                'adminWorkshopsIndex' => route('admin.workshops.index'),
                'adminWorkshopsCreate' => route('admin.workshops.create'),
                'adminWorkshopsStats' => route('admin.workshops.stats'),
            ],
            'flash' => [
                'success' => fn () => $request->session()->get('success'),
                'error' => fn () => $request->session()->get('error'),
            ],
        ]);
    }
}
